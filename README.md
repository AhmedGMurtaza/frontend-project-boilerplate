<!-- PROJECT SETUP INSTRUCTIONS

=====================
.eslinerc
=====================
    To setup consistent coding rules defined for all Javascript coding files

=====================
.gitignore
=====================
    Ignore files to be commited to git.

=====================
.package.json
=====================
    Used for npm configutation and corresponding packages installation

=====================
BOWER (.bowerrc)
=====================
Bower can manage components that contain HTML, CSS, JavaScript, fonts or even image files. Bower doesn’t concatenate or minify code or do anything else - it just installs the right versions of the packages you need and their dependencies.

    #bower configuration should be saved in .bowerrc
    #bower packages should be saved in bower.json

    used to store all project dependent packages/libraries

    #bower install <package>

    installs the project dependencies listed in bower.json
    bower install
    # registered package
    bower install jquery
    # GitHub shorthand
    bower install desandro/masonry
    # Git endpoint
    bower install git://github.com/user/package.git
    # URL
    bower install http://example.com/script.js



-->
